langchain==0.2.6
langchain-openai==0.1.14
langchain-huggingface==0.0.3
langchain-community==0.2.6
langchain-chroma==1.0.0
chromadb==1.3.4
langchain-core==0.2.43
langchain-google-community==1.0.8
langchain-text-splitters==0.2.4
langchain-markitdown==0.1.6
openai>=1.40.0
python-dotenv==1.0.1
# openai  # not referenced anywhere in the codebase
# pyarrow  # no imports
# pandas  # no imports
# colored  # no imports
# pdfminer-six  # no imports
# pdf2image  # no imports
# Pillow  # no imports
# pytesseract  # no imports
# google-api-python-client>=2.100.0  # no imports
# lxml  # no imports
# pymupdf  # no imports
# faiss-cpu  # no imports
# colorlog  # no imports
# sentence_transformers  # no imports
# pyperclip  # no imports
# requests  # no imports
# bs4  # no imports
retry==0.9.2
pytest==9.0.0
flask==3.0.0
flask-cors==4.0.0
spacy>=3.7.0
